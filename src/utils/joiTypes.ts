// Since this is an optional peer dependency, we use a @ts-ignore so
// the users of the library dont get a compile error if they dont have it installed.
// @ts-ignore
export type { Description, Schema, ObjectSchema } from 'joi';
